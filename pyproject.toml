[project]
name = "hand-gesture-recognition"
version = "0.1.0"
description = "Hand gesture recognition using CNN"
requires-python = ">=3.11"
dependencies = [
    "opencv-python",
    "numpy",
    "torch",
    "scikit-learn",
    "matplotlib",
    "seaborn",
    "mediapipe==0.10.9",
]
